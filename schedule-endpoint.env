# Production schedule endpoint. This file is committed — if the schedule data
# ever moves to a new home, update this value and commit; no source edit needed.
#
# For local testing without touching this file, override via SCHEDULE_URL in
# local.env (gitignored) instead — that takes precedence over this file.
# See docs/CLAUDE.md "Schedule data pipeline".
SCHEDULE_URL=https://next-caltrain-pwa.appspot.com/schedule.json
