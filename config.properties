# Committed app configuration -- defaults that ship with the app unless
# overridden per-developer in local.env (gitignored, sim/test-only overrides
# -- see docs/SCHEDULE_ENDPOINT.md). Edit and commit this file directly to
# change a default for everyone (e.g. if the schedule data ever moves to a
# new home). Room for more keys here later; not schedule-only.
#
# Sourced as shell KEY=value by sim.sh, same simple format the Kotlin
# sibling reads as Java Properties from its own config.properties.
# See docs/COWORK.md "Schedule data pipeline".
SCHEDULE_URL=https://next-caltrain-pwa.appspot.com/feed/schedule.json
